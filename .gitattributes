# Auto detect text files and perform LF normalization
* text=auto

# Git LFS tracking with forced Linux line endings
Data/911.csv filter=lfs diff=lfs merge=lfs -text
database/*.sql filter=lfs diff=lfs merge=lfs -text eol=lf